import { Backend } from "../core/backend";
declare global {
    namespace NodeJS {
        interface Global {
            kuzzle: any;
            app: Backend;
            NODE_ENV: string;
        }
    }
}
export {};
