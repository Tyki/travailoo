import { SearchResultBase } from "./SearchResultBase";
import { KDocumentContentGeneric, KHit } from "../../types";
export declare class DocumentSearchResult<TKDocumentContent extends KDocumentContentGeneric> extends SearchResultBase<KHit<TKDocumentContent>> {
    /**
     * @param {Kuzzle} kuzzle
     * @param {object} query
     * @param {object} options
     * @param {object} result
     */
    constructor(kuzzle: any, query: any, options: any, result: any);
}
